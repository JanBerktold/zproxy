package main

// represents a higher-level connection. consumes parsed packages (ZkRequest, ZkResponse).
// responsible for sending initial packets to setup connection and invoking the handler based on the current node.
type Session struct {

}

